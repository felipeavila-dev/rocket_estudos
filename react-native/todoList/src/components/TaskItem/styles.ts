import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    marginTop: 8,
    backgroundColor: '#333333',
    borderRadius: 8,

  },
  text: {
    flex: 1,
    flexWrap: 'wrap',
  }
});